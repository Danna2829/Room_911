# Frontend Room_911

Frontend React de la sala de control. Las pantallas y carpetas de dominio están en español.

Desde la raíz del proyecto:

```bash
pnpm --dir front install
pnpm --dir front start
```

El backend debe estar disponible en `http://localhost:8080`. Para configurar otra URL, define `REACT_APP_API_URL`.
